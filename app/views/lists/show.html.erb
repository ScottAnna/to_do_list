<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>

<p>
  <strong>Tasks:</strong>
  <ul>
    <% @list.tasks.each do |task| %>
      <li>
        <%= task.order_number %>.
        <%= task.name %>
        <%= task.due %>
      </li>
    <% end %>
  </ul>
</p>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
